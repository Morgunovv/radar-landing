# 🚀 Инструкция по деплою на Vercel

## Быстрый старт

### Вариант 1: Через Vercel Dashboard (Рекомендуется)

1. **Подготовка репозитория**
   - Загрузите папку `landing/` в отдельный GitHub репозиторий
   - Или создайте подпапку в текущем репозитории

2. **Деплой на Vercel**
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите **"New Project"**
   - Импортируйте ваш GitHub репозиторий
   - В настройках проекта укажите:
     - **Root Directory**: `landing` (если landing в подпапке)
     - **Framework Preset**: Next.js
   - Нажмите **"Deploy"**

3. **Готово!**
   - Vercel автоматически определит Next.js
   - Создаст билд и задеплоит
   - Дамейн будет доступен сразу

### Вариант 2: Через Vercel CLI

```bash
# Перейдите в папку landing
cd landing

# Установите зависимости
npm install

# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите в аккаунт
vercel login

# Деплой
vercel

# Для продакшена
vercel --prod
```

## Настройка переменных окружения

На Vercel переменные окружения обычно не требуются для статического лэндинга, но если нужно:

1. Зайдите в **Project Settings → Environment Variables**
2. Добавьте нужные переменные (если потребуется)

## Настройка домена

1. Зайдите в **Project Settings → Domains**
2. Добавьте свой домен или используйте домен от Vercel (`.vercel.app`)

## Перед деплоем: Обязательно измените

### 1. Ссылки на бота

Найдите и замените `radar_telegram_bot` на реальный username вашего Telegram бота в файлах:

- `components/Navbar.tsx`
- `components/Hero.tsx`
- `components/Pricing.tsx`
- `components/CTA.tsx`
- `components/Footer.tsx`

### 2. Контактная информация

В `components/Footer.tsx` замените:
- `support@example.com` на ваш реальный email

### 3. Метаданные

В `pages/index.tsx` обновите:
- `<title>` — заголовок страницы
- `<meta name="description">` — описание для SEO

## Проверка локально

Перед деплоем обязательно протестируйте локально:

```bash
cd landing
npm install
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) и проверьте:
- ✅ Все ссылки работают
- ✅ Анимации плавные
- ✅ Адаптивность на мобильных
- ✅ Все тексты отображаются корректно

## Проблемы и решения

### Ошибка: "Module not found"
```bash
# Удалите node_modules и установите заново
rm -rf node_modules package-lock.json
npm install
```

### Ошибка при билде на Vercel
- Проверьте версию Node.js в `package.json`
- Убедитесь, что все зависимости установлены
- Проверьте логи билда в Vercel Dashboard

### Стили не применяются
- Проверьте `tailwind.config.js`
- Убедитесь, что `postcss.config.js` настроен правильно
- Проверьте импорт стилей в `pages/_app.tsx`

## Автоматический деплой

Vercel автоматически деплоит при каждом push в основную ветку GitHub.

Для настройки:
1. В Vercel Dashboard → Settings → Git
2. Убедитесь, что подключен правильный репозиторий
3. Настройте ветку для продакшена (обычно `main` или `master`)

## Полезные ссылки

- [Документация Vercel](https://vercel.com/docs)
- [Документация Next.js](https://nextjs.org/docs)
- [Документация Tailwind CSS](https://tailwindcss.com/docs)

